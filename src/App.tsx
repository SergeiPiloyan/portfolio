import { Box } from "@mui/material";

const styles = {
  background: "blue",
  width: "100%",
  height: "100%",
};

const App = () => {
  return <Box sx={styles}>My Portfolio</Box>;
};

export default App;
